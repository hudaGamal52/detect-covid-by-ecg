CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    firstname VARCHAR(50),
    lastname VARCHAR(50),
    phone_number VARCHAR(20),
    email VARCHAR(100),
    password VARCHAR(255)
);
